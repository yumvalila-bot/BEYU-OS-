{
  "name": "@beyu/agriculture-types",
  "version": "1.0.0",
  "description": "BEYU AGRICULTURE OS — canonical agriculture domain contracts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist",
    "test": "node --test --require @swc-node/register \"src/**/*.test.ts\""
  },
  "devDependencies": {
    "@swc-node/register": "^1.10.9",
    "@swc/core": "^1.10.1",
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0"
  }
}
