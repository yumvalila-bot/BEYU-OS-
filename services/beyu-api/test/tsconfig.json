{
  "//": "Tests live outside the build tsconfig's `include`, so tsx would otherwise fall back to default compiler options and emit ES decorators. NestJS requires the legacy decorator semantics.",
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": {
    "module": "commonjs",
    "moduleResolution": "node",
    "types": ["node"],
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "noEmit": true
  },
  "include": ["**/*.ts", "../src/**/*.ts"]
}
